{
  "name": "kaji-opencode-relay",
  "version": "0.1.0",
  "description": "Universal relay for OpenCode — route AI coding sessions to any channel",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./types": "./dist/types.js",
    "./adapter": "./dist/adapter.js",
    "./store": "./dist/store.js",
    "./schemas": "./dist/schemas.js"
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts src/types.ts src/adapter.ts src/store.ts src/schemas.ts --outdir dist --format esm --target node --external @opencode-ai/sdk --external zod --splitting",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "peerDependencies": {
    "@opencode-ai/sdk": "^1.2.5"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@opencode-ai/sdk": "^1.2.5",
    "bun-types": "latest",
    "typescript": "^5.4.5"
  }
}
