{
  "name": "kaji-opencode-relay",
  "version": "0.2.4",
  "description": "Universal relay for OpenCode — route AI coding sessions to any channel",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./types": "./dist/types.js",
    "./adapter": "./dist/adapter.js",
    "./store": "./dist/store.js",
    "./schemas": "./dist/schemas.js",
    "./debug": "./dist/debug/adapter.js"
  },
  "bin": {
    "kaji-opencode-relay-debug": "./bin/debug.ts"
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts src/types.ts src/adapter.ts src/store.ts src/schemas.ts src/debug/adapter.ts src/debug/cli.ts src/debug/renderer.ts --outdir dist --format esm --target node --external @opencode-ai/sdk --external zod --splitting && tsc -p tsconfig.build.json && node -e \"const fs=require('fs');for(const f of['dist/types.js','dist/schemas.js']){if(!fs.existsSync(f))continue;let c=fs.readFileSync(f,'utf8');const lines=c.split('\\n');const last=lines.findLastIndex(l=>l.startsWith('export {'));const first=lines.findIndex(l=>l.startsWith('export {'));if(last>first){lines.splice(last,1);fs.writeFileSync(f,lines.join('\\n'));}}\"",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "prepublishOnly": "bun run build"
  },
  "peerDependencies": {
    "@opencode-ai/sdk": "^1.2.5"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@opencode-ai/sdk": "^1.2.5",
    "bun-types": "latest",
    "typescript": "^5.4.5"
  }
}
